services:
  wordpress:
    image: wordpress:latest
    container_name: wordpress
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${MYSQL_USER}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD}
      WORDPRESS_DB_NAME: ${MYSQL_DATABASE}
      WORDPRESS_TABLE_PREFIX: ${WORDPRESS_TABLE_PREFIX}
    volumes:
      - wordpress_data:/var/www/html
      - ./wp-content:/var/www/html/wp-content
    depends_on:
      db:
        condition: service_started
    networks:
      - wordpress-network
      - default
    labels:
      # Catch-all configuration for 500+ domains
      # This works alongside Coolify's domain-specific labels
      - "traefik.enable=true"
      - "traefik.http.middlewares.gzip.compress=true"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.permanent=true"
      # Catch-all HTTPS router (catches domains not explicitly configured in Coolify)
      - "traefik.http.routers.wordpress-catchall-https.rule=HostRegexp(`{host:.+}`) && PathPrefix(`/`)"
      - "traefik.http.routers.wordpress-catchall-https.entrypoints=https"
      - "traefik.http.routers.wordpress-catchall-https.middlewares=gzip"
      - "traefik.http.routers.wordpress-catchall-https.tls.certresolver=letsencrypt"
      - "traefik.http.routers.wordpress-catchall-https.tls=true"
      - "traefik.http.routers.wordpress-catchall-https.service=wordpress"
      - "traefik.http.routers.wordpress-catchall-https.priority=1"
      # Catch-all HTTP router (redirects to HTTPS)
      - "traefik.http.routers.wordpress-catchall-http.rule=HostRegexp(`{host:.+}`) && PathPrefix(`/`)"
      - "traefik.http.routers.wordpress-catchall-http.entrypoints=http"
      - "traefik.http.routers.wordpress-catchall-http.middlewares=redirect-to-https"
      - "traefik.http.routers.wordpress-catchall-http.service=wordpress"
      - "traefik.http.routers.wordpress-catchall-http.priority=1"
      # Service - use container name for auto-discovery
      - "traefik.http.services.wordpress.loadbalancer.server.port=80"
    
  db:
    image: mariadb:latest
    container_name: wordpress_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - wordpress-network
    # Healthcheck removed - using service_started instead for more reliable startup
    # WordPress will retry database connection automatically

volumes:
  wordpress_data:
  db_data:

networks:
  wordpress-network:
    driver: bridge
  default:
    external: true
    name: coolify

